# macOS Finder metadata. These appear in every image folder and are easy to
# commit by accident with `git add -A`.
.DS_Store
._*

# Editor and OS noise
Thumbs.db
*.swp

# Node dependencies (sharp, used to build image derivatives). Installed with
# `npm install`; not committed. package.json / package-lock.json are.
node_modules/
